{
  "name": "sls-simpleversioning",
  "version": "1.0.0",
  "description": "A simple example on how to use the ShapeDiver Platform Backend API for strong authorization of calls to the Geometry Backend API, and identifying models based on their slug.",
  "main": "handler.js",
  "scripts": {
    "cli": "ts-node src/handlers/cli",
    "local": "export AWS_PROFILE=serverless_cli; sls invoke local -f process",
    "sls": "export AWS_PROFILE=serverless_cli; sls",
    "slsdebug": "export AWS_PROFILE=serverless_cli; export SLS_DEBUG=* sls",
    "logs": "export AWS_PROFILE=serverless_cli; sls logs -f process"
  },
  "dependencies": {
    "@shapediver/sdk.geometry-api-sdk-v2": "^1.0.26",
    "@shapediver/sdk.platform-api-sdk-v1": "^2.9.14",
    "yargs": "^17.5.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.103",
    "@types/node": "^18.7.16",
    "@types/serverless": "^3.12.8",
    "serverless-bundle": "^5.5.0",
    "serverless-plugin-log-retention": "^2.0.0"
  },
  "author": "ShapeDiver GmbH",
  "license": "MIT"
}
